{var $cacheTags = [ZaxCMS\Model\CMS\Service\CategoryService::CACHE_TAG]}

{cache 'category-' . $category->id . '-sidebar', tags => $cacheTags}
	{foreach array_reverse($ancestors) as $ancestor}
		{if !$ancestor->sidebarParent || $ancestor->depth === 0}
			{$ancestor->sidebarContent|texy|noescape}
			{breakIf !$ancestor->sidebarParent || $ancestor->depth === 0}
		{/if}
	{/foreach}
{/cache}